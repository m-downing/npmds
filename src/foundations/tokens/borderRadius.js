/**
 * Design system border radius tokens
 * This is the single source of truth for all border radius values in the design system.
 */

const borderRadius = {
  xs: '0.125rem',
  sm: '0.275rem',
  md: '0.375rem',
  lg: '0.5rem',
  xl: '1rem',
};

// CommonJS default export only
module.exports = { borderRadius }; 